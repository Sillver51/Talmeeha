# Talmeeha runs on a WSL /mnt/d (DrvFs) mount where npm extraction is sometimes
# incomplete. The real safety net is scripts/check-deps.mjs (runs on `predev`);
# these settings only cut install noise and speed up the auto-repair reinstall by
# reusing the verified local cache. They do NOT change dependency resolution.
fund=false
prefer-offline=true
